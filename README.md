# Behind U - 香港 AI 買嘢神器

一個香港本地 AI 廣告搜索平台，支援廣東話輸入搜尋，為用戶提供本地商戶廣告信息。

## 功能特色

- 🖤 黑奢風設計，背景 #000000，白色文字，重點綠色 #00ff99
- 🔍 智能搜尋，支援廣東話輸入
- 📱 手機優先響應式設計
- 💳 Stripe 支付集成（廣告發布收費）
- 🛡️ Supabase 後端（資料庫、認證、存儲）
- 📦 PWA 支援，可加到手機桌面

## 技術棧

- **前端**: Next.js 14 App Router + TypeScript + Tailwind CSS
- **後端**: Supabase（資料庫、認證、存儲）
- **支付**: Stripe
- **部署**: Cloudflare Pages

## 專案結構

```
behind-u-hk/
├── app/
│   ├── layout.tsx          # 根布局
│   ├── page.tsx           # 首頁
│   ├── globals.css        # 全局樣式
│   └── search/
│       └── page.tsx       # 搜尋結果頁面
├── components/
│   ├── SearchBox.tsx      # 搜尋框組件
│   └── AdCard.tsx         # 廣告卡片組件
├── lib/
│   └── supabase.ts        # Supabase 配置
├── api/
│   └── create-checkout/
│       └── route.ts       # Stripe 支付 API
├── package.json
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
└── .env.local
```

## 安裝與運行

1. 安裝依賴
```bash
npm install
```

2. 設定環境變數
複製 `.env.local` 並填入您的 Supabase 和 Stripe 密鑰：

```bash
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
STRIPE_SECRET_KEY=your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
```

3. 啟動開發服務器
```bash
npm run dev
```

在 [http://localhost:3000](http://localhost:3000) 開啟瀏覽器查看結果。

## 部署到 Cloudflare Pages

1. 將專案推送到 GitHub
2. 在 Cloudflare Pages 中連接您的 GitHub 倉庫
3. 設定構建配置：
   - 構建命令: `npm run build`
   - 構建輸出目錄: `.next`
4. 設定環境變數
5. 部署

## 頁面功能

### 首頁 (/)
- 超大標題 "Behind U"
- Slogan: "你想要的,背後都有"
- 超大搜尋欄（支援廣東話輸入）

### 搜尋結果頁面 (/search)
- 顯示廣告卡片網格
- 每個卡片包含：商戶名稱、廣告標題、描述、聯繫方式、WhatsApp 快捷按鈕

### 商家 Dashboard (/dashboard)
- 商家註冊登入
- 廣告管理：上傳新廣告、編輯現有廣告
- Stripe 支付：發布廣告需付費 HK$50
- 廣告狀態追蹤

### 管理員後台 (/admin)
- 管理員登入
- 廣告審核：一鍵通過/拒絕/刪除
- 用戶管理
- 收入統計

## 開發狀態

- ✅ 基本 Next.js 14 項目結構
- ✅ Tailwind CSS 黑奢風設計
- ✅ Supabase 配置
- ✅ Stripe 支付 API
- ✅ 基本頁面和組件
- 🔄 商家 Dashboard（進行中）
- 🔄 管理員後台（進行中）
- 🔄 PWA 配置（進行中）

## 貢獻

歡迎提交 Issue 和 Pull Request 來改善這個專案。

## 授權

MIT License